import 'package:flutter/material.dart';

class DataPembayaranBody extends StatelessWidget {
  const DataPembayaranBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Coming Soon'),
    );
  }
}
