import 'package:flutter/material.dart';

class DataLKNBody extends StatelessWidget {
  const DataLKNBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Coming Soon'),
    );
  }
}
